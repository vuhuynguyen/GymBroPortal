# GymBro Portal (`GymBroPortal/`)

Angular 21 · standalone components · signals · OnPush · PrimeNG — the web client for the **GymBro**
multi-tenant fitness-coaching platform.

**Start with the documentation, not the code:**

- System single source of truth → [`../docs/README.md`](../docs/README.md)
- Frontend structure / UI rules / state & HTTP / routing → [`docs/FRONTEND.md`](docs/FRONTEND.md)
- Repo conventions for AI agents → [`CLAUDE.md`](CLAUDE.md)

Run: `npm install && ng serve` (proxies `/api` → the API). Details in [`docs/FRONTEND.md`](docs/FRONTEND.md).
